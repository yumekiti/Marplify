version: '3'

services:
  app:
    image: node:14.21.3-alpine
    working_dir: /app
    volumes:
      - ./web:/app
    ports:
      - '3000:3000'
    command: >
      sh -c "
        yarn &&
        yarn start
      "
